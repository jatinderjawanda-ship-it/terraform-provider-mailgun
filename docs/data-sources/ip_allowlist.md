---
page_title: "mailgun_ip_allowlist Data Source - mailgun"
subcategory: "IP Allowlist"
description: |-
  Retrieves all IP allowlist entries for the Mailgun account. IP allowlisting restricts API key and SMTP credential usage to specific IP addresses.
---

# mailgun_ip_allowlist (Data Source)

Retrieves all IP allowlist entries for the Mailgun account. IP allowlisting restricts API key and SMTP credential usage to specific IP addresses.

## Example Usage

```terraform
# List all IP allowlist entries
data "mailgun_ip_allowlist" "all" {}

# Output all allowlisted IPs
output "allowlisted_ips" {
  value = [for entry in data.mailgun_ip_allowlist.all.entries : entry.address]
}

# Output entries with descriptions
output "allowlist_details" {
  value = {
    for entry in data.mailgun_ip_allowlist.all.entries :
    entry.address => entry.description
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `entries` (Attributes List) List of IP allowlist entries. (see [below for nested schema](#nestedatt--entries))
- `id` (String) The unique identifier for this data source.

<a id="nestedatt--entries"></a>
### Nested Schema for `entries`

Read-Only:

- `address` (String) The IP address or CIDR range that is allowlisted.
- `description` (String) The description of the IP allowlist entry.
