---
page_title: "mailgun_subaccount Data Source - mailgun"
subcategory: "Subaccounts"
description: |-
  Use this data source to look up an existing Mailgun subaccount by ID. Subaccounts are child accounts that share the same plan as the primary account but have their own domains, users, and settings.
---

# mailgun_subaccount (Data Source)

Use this data source to look up an existing Mailgun subaccount by ID. Subaccounts are child accounts that share the same plan as the primary account but have their own domains, users, and settings.

~> **Note:** Subaccounts are available as data sources only. The Mailgun API does not support deleting subaccounts (only disabling), which would violate Terraform's resource lifecycle expectations.

## Example Usage

```terraform
# Look up a subaccount by ID
data "mailgun_subaccount" "example" {
  id = "12345678-1234-5678-1234-123456789abc"
}

# Use the subaccount information
output "subaccount_name" {
  value = data.mailgun_subaccount.example.name
}

output "subaccount_status" {
  value = data.mailgun_subaccount.example.status
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The unique identifier of the subaccount.

### Read-Only

- `name` (String) The name of the subaccount.
- `status` (String) The status of the subaccount: 'enabled' or 'disabled'.
