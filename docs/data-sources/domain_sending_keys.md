---
page_title: "mailgun_domain_sending_keys Data Source - mailgun"
subcategory: "Keys"
description: |-
  Lists all domain sending keys for a specific Mailgun domain.
---

# mailgun_domain_sending_keys (Data Source)

Lists all domain sending keys for a specific Mailgun domain.

## Example Usage

```terraform
# List all sending keys for a domain
data "mailgun_domain_sending_keys" "all" {
  domain = "mail.example.com"
}

output "sending_key_count" {
  value = data.mailgun_domain_sending_keys.all.total_count
}

output "sending_key_ids" {
  value = [for k in data.mailgun_domain_sending_keys.all.keys : k.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) The domain to list sending keys for.

### Read-Only

- `keys` (Attributes List) List of domain sending keys. (see [below for nested schema](#nestedatt--keys))
- `total_count` (Number) Total number of sending keys for this domain.

<a id="nestedatt--keys"></a>
### Nested Schema for `keys`

Read-Only:

- `created_at` (String) Timestamp when the key was created.
- `description` (String) Human-readable description of the key.
- `expires_at` (String) Timestamp when the key expires, if applicable.
- `id` (String) The unique identifier of the sending key.
- `is_disabled` (Boolean) Whether the key is disabled.
